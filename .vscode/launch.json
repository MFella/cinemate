{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug backend",
            "request": "launch",
            "type": "node",
            "runtimeExecutable": "npx",
            "runtimeArgs": [
                "nx",
                "run",
                "backend:serve"
            ],
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}/apps/backend"
        }
    ]
}